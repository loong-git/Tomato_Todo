# 番茄TODO 优化与新功能计划

## 一、视觉设计改进

### 1.1 字体与缩放系统
- **问题**：时间显示使用 `vmin` 单位，缩放时字间距不美观
- **方案**：改用 `vw` 为主单位，配合 `clamp()` 实现平滑缩放
- **文件**：`src/components/TimerDisplay.vue`、`src/components/FocusWindow.vue`

### 1.2 主题系统增强
- **问题**：配色单一，缺少强调色渐变
- **方案**：
  - 增加 `--accent` 变量，番茄红向珊瑚色渐变
  - 深色主题增加柔和的边框发光效果
  - 浅色主题增加微妙的纸张纹理
- **文件**：`src/App.vue`、`src/components/FocusWindow.vue`

### 1.3 空状态设计
- **问题**：任务列表为空时无引导
- **方案**：添加引导性空状态插图 + 快捷操作提示
- **文件**：`src/components/TaskList.vue`

---

## 二、用户体验优化

### 2.1 进度环增强
- **问题**：进度变化时缺乏视觉反馈
- **方案**：
  - 添加最后 1 分钟的呼吸闪烁动画
  - 完成时添加弹性缩放动画
  - 进度条颜色随时间渐变
- **文件**：`src/components/TimerDisplay.vue`
- **状态**：✅ 已完成

### 2.2 番茄完成激励
- **问题**：完成番茄后只是数字 +1，缺乏成就感
- **方案**：
  - 完成时添加番茄爆炸粒子动画（余烬效果）
  - 连胜计数徽章（3+ 铜牌🔥，5+ 银牌✨，10+ 金牌🌟）
  - 每日首次番茄的特别动效（金色光晕）
  - 完成时自动切换到前台显示动画
- **文件**：`src/components/CelebrationOverlay.vue`、`src/components/TimerControls.vue`、`src/stores/timer.ts`
- **状态**：✅ 已完成

### 2.3 停止音频按钮
- **位置**：TimerControls 下方
- **逻辑**：点击开始/暂停后显示，点击后停止音频并隐藏
- **文件**：`src/components/TimerControls.vue`
- **状态**：✅ 已完成

### 2.3 键盘快捷键
- **问题**：完全无键盘操作支持
- **方案**：
  - `Space` - 开始/暂停计时器
  - `R` - 重置当前计时器
  - `Esc` - 退出专注模式
  - `S` - 打开设置面板
  - `N` - 新建任务
  - `1/2/3` - 切换模式
- **文件**：`src/App.vue`、`src/components/SettingsPanel.vue`
- **状态**：✅ 已完成

---

## 三、核心功能增强

### 3.1 多任务并行支持
- **问题**：当前只能选一个任务
- **方案**：
  - `currentTaskId` → `currentTaskIds: string[]`
  - 点击任务切换选中状态（支持多选）
  - 番茄完成时给所有选中任务 +1
  - Done 按钮点击时从选中列表移除
- **文件**：
  - `src/types/index.ts`
  - `src/stores/timer.ts`
  - `src/components/TaskList.vue`
  - `src/App.vue`

### 3.2 自适应专注时长
- **问题**：固定时长不够灵活
- **方案**：
  - 记录用户历史专注数据
  - 根据完成率智能调整专注时长（20-50 分钟区间）
  - 提供手动模式/自动模式切换
- **文件**：`src/stores/settings.ts`、`src/stores/stats.ts`

### 3.3 专注质量分析
- **问题**：仅记录番茄数量，缺少质量指标
- **方案**：
  - 记录中断次数
  - 计算专注效率 = 实际专注时长 / 计划专注时长
  - 生成周报视图
- **文件**：`src/stores/stats.ts`

---

## 四、新功能

### 4.1 打卡表
- 替代热力图，展示每日专注时长
- 放在 StatsDisplay 中
- 支持周视图切换
- **状态**：✅ 已完成

### 4.2 自定义音效
- 内置番茄钟音效（森林、海浪、雨声、钟声）
- 支持多音效混音
- 专注结束 / 休息开始不同提示音

### 4.3 勿扰模式集成
- Windows Focus Assist 联动
- 专注模式开启时自动启用

### 4.4 系统托盘
- 最小化到托盘
- 显示当前番茄状态（倒计时 + 图标颜色）
- 右键菜单：显示窗口/退出
- 番茄图标
- **状态**：✅ 已完成

### 4.5 数据导出增强
- CSV 格式导出
- 周报 PDF 生成
- 数据可视化图表导出

---

## 实施优先级

| 优先级 | 功能 | 改动文件数 |
|--------|------|-----------|
| P0 | 多任务并行支持 | 4 |
| P0 | 键盘快捷键 | 1 |
| P1 | 番茄完成激励动画 | 3 |
| P1 | 停止音频按钮 | 1 |
| P1 | 进度环增强 | 1 | ✅ |
| P1 | 系统托盘 | 2 | ✅ |
| P2 | 打卡表 | 1 | ✅ |
| P2 | 自定义音效 | 2 | ✅ |
| P3 | 自适应时长 | 3 |
| P3 | 数据导出增强 | 2 |

---

## 建议实施顺序

所有主要功能已完成！🎉
